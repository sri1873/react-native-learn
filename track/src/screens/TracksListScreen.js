import { StyleSheet, View, Text,Button } from 'react-native'

const TracksListScreen = () => {
    return (
        <View>
            <Text>TracksList Screen</Text>
            <Button title='s'/>
        </View>
    );
}

const styles = StyleSheet.create({

})

export default TracksListScreen;